{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": [],
      "inputs": [
        "prisma/schema.prisma",
        "prisma/migrations/**",
        "prisma.config.js"
      ]
    },
    "test": {
      "dependsOn": ["build"],
      "outputs": []
    }
  }
}
