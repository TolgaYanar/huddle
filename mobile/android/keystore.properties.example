# Copy to `keystore.properties` (same folder as this file) and fill in values.
# Do NOT commit `keystore.properties` or your keystore.
#
# storeFile can be a relative path from `mobile/android/`.

storeFile=upload-keystore.jks
storePassword=CHANGE_ME
keyAlias=upload
keyPassword=CHANGE_ME
